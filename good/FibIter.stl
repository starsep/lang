list<int> range(int n) {
    if (n <= 0) {
        return int[];
    } else {
        n--;
        return n $ range(n);
    }
}

int fib(int n) {
    int x = 0, y = 1;
    for i in range(n) {
        int sum = x + y;
        x = y;
        y = sum;
    }
    return y;
}

void main() {
    let n = 9;
    print(fib(n));
}
