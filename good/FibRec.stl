int fib(int n) {
    if n <= 1 {
        return 1;
    }
    return fib(n - 1) + fib(n - 2);
}

void main() {
    assert(fib(0) == 1);
    assert(fib(1) == 1);
    assert(fib(2) == 2);
    assert(fib(8) == 34);
    assert(fib(9) == 55);
}
